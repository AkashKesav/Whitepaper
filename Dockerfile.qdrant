# Qdrant Service for Railway
FROM qdrant/qdrant:latest
EXPOSE 6333 6334
# Remove healthcheck - Qdrant image may not have necessary tools
# Railway will detect service via exposed ports
CMD ["./qdrant"]
