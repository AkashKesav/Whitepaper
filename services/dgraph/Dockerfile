# DGraph Service Configuration for Railway
# Uses the official DGraph standalone image (Zero + Alpha combined)
# The image has built-in entrypoint, no CMD needed

FROM dgraph/standalone:latest

# Expose ports for gRPC and HTTP
EXPOSE 8080 9080
